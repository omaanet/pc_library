# Define the root of your project
packages:
  - '.'

# List the packages allowed to run build scripts
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true

# Add this section below your existing configs:
peerDependencyRules:
  allowAny:
    - 'eslint'
